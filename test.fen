extern (add x y)
extern (sub x y)
extern (mul x y)

extern (slt x y) //signed less than

extern (set_byte ptr value)
extern (get_byte ptr)

extern (printc char)
extern (printn string length)

private (strlen string; len) {
	let len = 0
	while (get_byte (add string len)) let len = (add len 1)
	return len
}

private (print string) {
	(printn string (strlen string))
}

public (main) {
	(print "Hello World!\n")
	return 0
}
